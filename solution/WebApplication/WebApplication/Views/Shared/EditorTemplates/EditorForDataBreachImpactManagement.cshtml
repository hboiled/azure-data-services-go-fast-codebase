@model WebApplication.Models.Customisations.WizardReference.InputSelectionOptionsFor<WebApplication.Models.Customisations.WizardReference.DataBreachImpactManagement>
<div class="form-group row col-12">
    @for (int i = 0; i < Model.Options.Count; i++)
    {
        <div class="col-6">
            <input asp-for="Options[i].IsSelected" />
            <label class="control-label">
                @Html.DisplayFor(m => m.Options[i].Option)
            </label>
            <br>
        </div>
    }

</div>