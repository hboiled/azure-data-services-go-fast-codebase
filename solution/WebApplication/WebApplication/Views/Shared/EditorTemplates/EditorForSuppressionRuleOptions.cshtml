@model WebApplication.Models.Customisations.WizardReference.InputSelectionOptionsFor<WebApplication.Models.Customisations.WizardReference.SuppressionRuleOptions>
<div class="form-group">
    @for (int i = 0; i < Model.Options.Count; i++)
    {
        <input asp-for="Options" type="radio" value="@Model.Options[i].Option" />
        <label class="control-label">
            @Html.DisplayFor(m => m.Options[i].Option)
        </label>
        <br>
    }

</div>